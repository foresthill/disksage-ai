# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / temp
*.swp
*~
*.tmp

# DiskSage runtime data (created under $HOME/.disksage by default)
.disksage/

# Built macOS app bundle (generate with scripts/make-app.sh)
DiskSage.app/

# Rust build output (engine/ PoC, desktop/src-tauri)
target/
